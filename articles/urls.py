from django.conf.urls import url, include
from django.contrib import admin
from . import views


app_name = 'articles'
urlpatterns = [
#    url('', views.index, name='index'),
   # url('<int:article_id>/', views.index, name='detail')
]
